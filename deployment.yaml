apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-flask-deployment
  labels:
    app: my-flask-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-flask-app
  template:
    metadata:
      labels:
        app: my-flask-app
    spec:
      containers:
      - name: my-flask-container
        image: my-flask-app:latest
        imagePullPolicy: Never # Use local image from Minikube's Docker daemon
        ports:
        - containerPort: 5000

